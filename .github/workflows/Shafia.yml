name: User2 Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: docker build -t myapp .

    - name: Login
      run: echo "${{ secrets.USER2_PASSWORD }}" | docker login -u "${{ secrets.USER2_USERNAME }}" --password-stdin

    - name: Tag
      run: docker tag myapp ${{ secrets.USER2_USERNAME }}/myapp:latest

    - name: Push
      run: docker push ${{ secrets.USER2_USERNAME }}/myapp:latest
